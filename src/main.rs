#[allow(unused_imports)]
use rust_playpen::snippets::{
    employee_automation, guess_number, median_and_mode, structs, temperature_converter, using_json,
};

fn main() {
    todo!("Run code snippets to see how they work.");
    // temperature_converter::run();
}
